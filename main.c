#include <stdio.h>
#include <string.h>
#include <unistd.h>

int main() 
    while (x=1) {
        char cwd[1024];
        getcwd(cwd, sizeof(cwd));
        printf("%s$", cwd);
        char result[100]; 
        scanf("%99s", result);
    }
}